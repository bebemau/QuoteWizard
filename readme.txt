The web client project is called WebClientMVC.  In the WebClientMVC project's web.config there is a configuration called ApiBaseUri that can be used to setup the base uri for the web api project.

If there is any error, the application logs to the folder c:\log.  QuoteWizard.log is the log for the web apis and WebClientMVC.log is the log for the web client.

